export interface Vehicle{
  ownerId: string;
  year: number;
  model: string;
  type: string;
  color: string;
  condition: string;
  engine: string;
  weight: number;
  mpg: number;
  ledLights: boolean;
}
