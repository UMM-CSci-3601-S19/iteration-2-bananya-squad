export interface User {
  _id?: object;
  userId: string;
  email: string,
  fullName: string;
  pictureUrl: string;
  lastName: string;
  firstName: string;
  phoneNumber?: string;
  description?: string;
}
